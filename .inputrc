#$if mode=vi
    #"\C-l": clear
    #bell-style: visible # 's'
#$endif

#$if Bash
  # F10 toggles mc on and off
  # Note Ctrl-o toggles panes on and off in mc
  #"\e[21~": "mc\C-M"

  # do history expansion when space entered
  Space: magic-space
#$endif
