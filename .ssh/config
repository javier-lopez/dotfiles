Host *
    #connection sharing
    ControlMaster auto
    ControlPath /tmp/ssh_mux_%h_%p_%r
    ControlPersist 3h
    #use local keys on remote machines
    ForwardAgent yes
    #keep the connection alive for finnecky servers
    TCPKeepAlive no
    ServerAliveInterval 60
    ServerAliveCountMax 10

Host buzz
    Port 1003
    #LocalCommand echo buzz
    #PermitLocalCommand yes

Host *.pagekite.me
    CheckHostIP no
    ProxyCommand /bin/nc -X connect -x %h:443 %h %p

Host msg.sh
    User admin

Host h.javier.io
    CheckHostIP no
    ProxyCommand /bin/nc -X connect -x %h:443 %h %p

Host home.javier.io
    CheckHostIP no
    ProxyCommand /bin/nc -X connect -x %h:443 %h %p

Host f.javier.io
    User admin

Host files.javier.io
    User admin

Host b.javier.io
    User admin

Host backup.javier.io
    User admin

Host ccand.cc #hostgator
    Port 2222
    PubkeyAuthentication no

Host bitbucket.org
    IdentityFile ~/.ssh/correo.io.id_rsa
